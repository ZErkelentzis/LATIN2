MoC for implicit moprhisms is completely flawed, e.g., includes acquire definies based on implicit morphisms from previos processing of the theory; or an implicit view is used while rechecking that very view
Better (?): every theory stores its incoming implicits (flattening; note that implicit structures may have to generate implicit structures); implicit views are tracked separately; only allow implicits among unions of named theories
cycle-check only needed after an implicit view
problem: how to handle dependency relation between modules?

ideally, a view is checked like a theory with one extra include (codomain) and a final totality check (domain)

realization = view into current theory ('include view' in analogy to 'include theory')
-> done, needs testing
 
hardcode andrewsdotright
 -> done, needs testing
 add andrewsDotRight variant that does not eat lambda/Pi

disable all jedit painting to chase down painting error
  all extensions disabled, seems not to help; now line invalidation disabled

multiple notations for a constant

document formats as interpretation hint preambles chosen by file ending
delimiters, variable type reservations as interpretation hint, in a theory
 should this be local?

preamble files in every folder of archive; documents that enrich document context

unicode-replacers to be used in mmttex
needs testing

definitional theory extensions

parametric views, e.g., for power set lattice

structures with parameters, e.g.,
structure equal {A: tp} : ?Equality = term = tm A
(may not be implicit?)

abstraction: variables are bound in order of use (is that unique if there are second-level implicit bindings?)

every var decl has list of conditions, e.g., sub/supertype bound, guards, ranges
concrete syntax unclear, but can be used internally
bound variables could be introduced through their condition: "x in A" becomes "x with x in A"; similar to how we anyway turn attributed variables into vardecls

preview method in presenter

resolve names dynamically in parsing (no notations for unapplied constants); use to skip in include steps in qualified names (also use this for names in views)

mark structure/include in view as implicit; effect: only composed morphism is implicit, not the whole strucutre

aliases and notations in include must be allowed

with T = {a conservative extension of parent+T} 